# Enviroment variables needed to connect to the LiveKit server.
LIVEKIT_API_KEY=<your_api_key>
LIVEKIT_API_SECRET=<your_api_secret>
LIVEKIT_URL=wss://<project-subdomain>.livekit.cloud

# Agent dispatch (https://docs.livekit.io/agents/server/agent-dispatch)
# Diana registers as a named agent ("diana"), which requires explicit dispatch.
# This must match AGENT_NAME in the agent (see agent/src/agent.py).
# (If left blank, app-config.ts falls back to "diana".)
AGENT_NAME=diana

# Internally used environment variables
NEXT_PUBLIC_APP_CONFIG_ENDPOINT=
SANDBOX_ID=
